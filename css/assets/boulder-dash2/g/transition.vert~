uniform float uniTime;

attribute float attCenterX;
attribute float attX;
attribute float attY;

void main() {
  float x = attCenterX + attX * sin(uniTime);
  float y = attY;
  gl_Position = vec4( x, y, 1.0, 1.0 );
}
