attribute float x;
attribute float y;
attribute float r;
attribute float g;
attribute float b;

varying vec3 color;

void main() {
  color = vec3( r, g, b );
  gl_Position = vec4( x, y, 0.0, 1.0 );
}
